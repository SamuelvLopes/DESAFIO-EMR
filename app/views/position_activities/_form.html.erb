<%= form_with(model: position_activity) do |form| %>
  <% if position_activity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(position_activity.errors.count, "error") %> prohibited this position_activity from being saved:</h2>

      <ul>
        <% position_activity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id_position, style: "display: block" %>
    <%= form.number_field :id_position %>
  </div>

  <div>
    <%= form.label :id_activy, style: "display: block" %>
    <%= form.number_field :id_activy %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
